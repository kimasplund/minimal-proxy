version: '3.8'

services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python-proxy
    ports:
      - "443:443"
    environment:
      - TARGET_HOST=192.168.14.50
    volumes:
      - ./certs:/app/certs
    restart: unless-stopped 